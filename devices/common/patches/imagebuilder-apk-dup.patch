--- a/target/imagebuilder/Makefile
+++ b/target/imagebuilder/Makefile
@@ -55,2 +55,2 @@ $(BIN_DIR)/$(IB_NAME).tar.zst: clean
 	find $(PACKAGE_SUBDIRS) -type f -name '*.apk' \
-		-exec cp -fpR -t $(PKG_BUILD_DIR)/packages/ {} +
+		-exec cp -fpnR -t $(PKG_BUILD_DIR)/packages/ {} +
